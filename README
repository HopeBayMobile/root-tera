How to install root-tera?
# connect android phone to PC, and adb connection is ok.
$ cd ./root-tera/
$ ./setup.sh install

How to uninstall root-tera?
# connect android phone to PC, and adb connection is ok.
$ cd ./root-tera/
$ ./setup.sh uninstall


File "./root-tera/tera"?
Shell script, tera daemon in Android.

Directory "./root-tera/files/"?
You MUST Overwrite files(depend on different Android version & mobile) to the directory.
Default files support Android 7.1.2 & 8.0.0.

File "./root-tera/files/hcfs"?
* git url:   git@gitlab.hopebaytech.com:gateway-2-0/hcfs.git
* branch:    feature/root-tera
* commit id: 9f79eeec67
